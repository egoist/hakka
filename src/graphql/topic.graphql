query topic($id: Int!) {
  topicById(id: $id) {
    id
    title
    html
    createdAt
    likesCount
    isLiked
    author {
      id
      username
      avatar
    }
    externalLink {
      url
      domain
    }
  }
}

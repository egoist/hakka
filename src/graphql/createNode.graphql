mutation createNode(
  $name: String!
  $description: String!
  $slug: String!
  $image: String
) {
  createNode(
    name: $name
    description: $description
    slug: $slug
    image: $image
  ) {
    id
    slug
  }
}
